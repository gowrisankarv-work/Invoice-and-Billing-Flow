/**
 * Organizations (tenants), their settings and user membership.
 */
@ApplicationModule(displayName = "Tenant")
package com.invoiceflow.tenant;

import org.springframework.modulith.ApplicationModule;
